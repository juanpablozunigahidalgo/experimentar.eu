name: Deploy to GitHub Pages

on:
  push:
    branches: [ master ] # Asegúrate de que tu rama se llame master o main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install, build, and upload
        uses: withastro/action@v3